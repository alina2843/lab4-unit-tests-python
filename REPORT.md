# 🔬 Лабораторная работа №4. Написание Unit-тестов

## 1. Исходные данные и инструменты
* **Язык программирования:** Python
* **Фреймворк для тестирования:** `unittest`
* **Тестируемый модуль:** `rectangle.py`
* **Репозиторий:** https://github.com/alina2843/lab4-unit-tests

## 2. План тестирования

### Цели и задачи тестирования
Целью является проверка корректности работы функций вычисления площади и периметра. [cite_start]Основная задача — реализация автоматизированных unit-тестов для модуля `rectangle.py` с использованием библиотеки `unittest`[cite: 34].

### Описание тестируемого продукта
[cite_start]Продукт представляет собой библиотеку на языке Python, содержащую математические функции для вычисления параметров геометрических фигур (площади и периметра)[cite: 35].

### Область тестирования
В тестирование входит функция `area(a, b)` модуля `rectangle.py`. [cite_start]Проверяется корректность математических вычислений при различных входных данных (целые числа, нуль)[cite: 36].

### Стратегия тестирования
[cite_start]Используется метод модульного тестирования (Unit Testing) с применением стандартной библиотеки Python `unittest`[cite: 11, 37].

### Критерии приемки
[cite_start]Тестирование считается успешным, если все написанные unit-тесты проходят без ошибок (статус OK) при запуске через командную строку[cite: 39].

### Ожидаемые результаты
[cite_start]При запуске тестов ожидается вывод сообщения `OK` от интерпретатора, подтверждающий, что фактический результат работы функций совпадает с ожидаемым[cite: 40].

## 3. Ход работы и результаты

### 3.1. Исходный код тестируемой функции (`src/rectangle.py`)
```python
def area(a, b):
    # Вычисляет площадь прямоугольника
    return a * b

def perimeter(a, b):
    # Вычисляет периметр прямоугольника
    return 2 * (a + b)